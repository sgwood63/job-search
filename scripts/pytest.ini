[pytest]
testpaths = tests
markers =
    e2e: all end-to-end tests — require real LinkedIn auth and Chrome/Firefox (local only)
    e2e_jd: JD fetch tests only — LinkedIn JDs (auth required) + public ATS URLs
    e2e_public: public ATS URL tests — no auth needed, safe for CI
