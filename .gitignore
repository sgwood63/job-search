# Applications — tracked in Google Drive, not Git
applications/

# Application tracker — contains personal application history
/application-tracker.md

# Base documents — personal resume PDFs and LinkedIn exports
base-documents/*.pdf
base-documents/*.html
base-documents/Sherman*
base-documents/Jobs*
base-documents/AI\ Architect*
base-documents/linkedin-job-listing-page.html

# Environment — contains local paths, never commit
.env
.env-backup

# MCP server config — contains real access keys, never commit
# Copy from .mcp.json.example and fill in real values
.mcp.json

# Authenticated browser sessions (per-user, never commit)
.auth/

# Auto-memory duplicate (canonical copy lives in memory/)
/MEMORY.md

# macOS
.DS_Store
**/.DS_Store

# Scratchpad
scripts/scratchpad
Job-Search-2026.code-workspace
