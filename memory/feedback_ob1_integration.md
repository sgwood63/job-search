---
name: feedback_ob1_integration
description: "MIGRATED — canonical DATA_BACKEND routing rules now live in policies/storage-routing/"
metadata:
  type: feedback
---

**Migrated to versioned skills.** The canonical OB1/DATA_BACKEND routing rule — MCP tool mapping, hard-stop protocol, upload routing (MCP vs REST), PDF-in-OB1 sequence — lives in `$APP_DIR/policies/storage-routing/`. Read the pinned version per its `skill.yaml` (interactive sessions: prefer `draft.md` if present).

Do not add new rules here — use `/skill draft storage-routing`.
