---
name: Application tracking rules
description: "MIGRATED — tracker lookup and two-file status rules now live in workflows/create-application/"
type: feedback
---

**Migrated to versioned skills.** The application tracking rules — check the tracker before acting on a company mention; the two-file rule (tracker + notes.md) for every status change; the 9 canonical status values — live in `$APP_DIR/workflows/create-application/` (Step 0 and "Status changes — Two-File Rule"). Read the pinned version per its `skill.yaml`.

Do not add new rules here — use `/skill draft create-application`.
